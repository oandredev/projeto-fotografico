Esses três arquivos são responsáveis pelo gerenciamento de upload e disponibilização de imagens armazenadas `localmente` na API, mantendo no banco de dados` apenas o caminho (URL)` de acesso aos arquivos.

- multer.config.js
- static.config.js
- upload-path.js
